# Oklahoma Topo Map — Python dependencies
# Install with:  pip install -r requirements.txt

numpy>=1.24
matplotlib>=3.7
rasterio>=1.3
geopandas>=0.14
osmnx>=1.7
shapely>=2.0
scipy>=1.11
requests>=2.31
pyogrio>=0.7       # replaces fiona — pre-built binary wheels available on all platforms

# For DEM download from USGS 3DEP (handles all tile fetching automatically)
py3dep>=0.17
rioxarray>=0.15
xarray>=2023.1
psutil>=5.9        # RSS memory monitoring during render
pikepdf>=8.0       # PDF compositing for parallel layer merge
